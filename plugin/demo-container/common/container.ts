import mdContainer from "markdown-it-container";

const demoBlockContainers = (options) => {
  const { component } = options;
  console.log('78787878demoBlockContainers', options);
  
};

export default demoBlockContainers;
